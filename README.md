<!-- TODO: Rename repo to footprint-game -->
# Footprint game

Measuring the executable footprint of Hello World across languages and compilation strategies.

## Rules

- Print `Hello, World!` and end with a newline.
- Exit with code 0 or equivalent.
- Print to standard output.
- Measured via GNU's `wc -c` or Python's `os.path.getsize()`.
